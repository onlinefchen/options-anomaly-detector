name: Daily Options Analysis

on:
  # CSV-CENTRIC WORKFLOW: Run daily at 4:00 PM Beijing Time
  # Purpose: Check if Polygon has uploaded today's CSV file and process it
  # CSV is THE CORE prerequisite - without it, analysis cannot proceed
  schedule:
    - cron: '0 8 * * *'  # 08:00 UTC = 4:00 PM Beijing Time (daily)
                         # Runs once per day to check and process CSV

  # Allow manual trigger
  workflow_dispatch:
    inputs:
      force_overwrite:
        description: '🔄 强制覆盖 (Force Overwrite) - 即使数据已存在也重新生成'
        required: false
        type: boolean
        default: false
      force_generate:
        description: '📜 强制生成历史数据 (Historical Data Generation)'
        required: false
        type: boolean
        default: false
      start_date:
        description: '📅 开始日期 (YYYY-MM-DD)'
        required: false
        type: string
        default: ''
      end_date:
        description: '📅 结束日期 (YYYY-MM-DD，留空则只生成开始日期)'
        required: false
        type: string
        default: ''
      days_back:
        description: '📊 或者：生成过去N个交易日（优先级最高）'
        required: false
        type: number
        default: 0

  # Run on push to main for testing
  push:
    branches: [ main ]
    paths:
      - 'src/**'
      - 'main.py'
      - '.github/workflows/**'

jobs:
  # Job 1: 数据下载与分析
  fetch-and-analyze:
    name: 📊 Data Fetching & Analysis
    runs-on: ubuntu-latest

    outputs:
      analysis-date: ${{ steps.get-date.outputs.date }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for historical analysis

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📅 Get current date
        id: get-date
        run: echo "date=$(TZ='Asia/Shanghai' date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: 📂 Restore historical data
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages-data
        continue-on-error: true

      - name: 💾 Copy historical JSON files
        run: |
          mkdir -p output
          if [ -d "gh-pages-data" ]; then
            echo "📊 Copying historical JSON files for 10-day analysis..."
            find gh-pages-data -name "*.json" -type f -exec cp {} output/ \; || true
            echo "✓ Historical data restored: $(ls output/*.json 2>/dev/null | wc -l) files"
          else
            echo "⚠️  No historical data found (first run?)"
          fi

      - name: 🔍 Check if data already exists
        id: check-data
        run: |
          echo "=================================================="
          echo "🔍 CHECKING IF DATA ALREADY EXISTS"
          echo "=================================================="

          CSV_DATE=$(TZ='Asia/Shanghai' python3 -c "from datetime import datetime; import sys; sys.path.insert(0, 'src'); from trading_calendar import get_previous_trading_day; print(get_previous_trading_day(datetime.now().strftime('%Y-%m-%d')))")
          echo "Target CSV date: $CSV_DATE"

          JSON_FILE="output/${CSV_DATE}.json"
          HTML_FILE="output/${CSV_DATE}.html"

          if [ -f "$JSON_FILE" ] && [ -f "$HTML_FILE" ]; then
            echo "✓ Data files found:"
            echo "  • JSON: $JSON_FILE"
            echo "  • HTML: $HTML_FILE"
            echo ""

            if [ "${{ github.event.inputs.force_overwrite }}" = "true" ]; then
              echo "🔄 Force overwrite enabled - will regenerate data"
              echo "skip_analysis=false" >> $GITHUB_OUTPUT
            else
              echo "⊘ Data already exists and force_overwrite not set"
              echo "⊘ Skipping analysis to avoid duplicate work"
              echo "skip_analysis=true" >> $GITHUB_OUTPUT
            fi
          else
            echo "⊘ Data files not found - proceeding with analysis"
            echo "skip_analysis=false" >> $GITHUB_OUTPUT
          fi

          echo "csv_date=$CSV_DATE" >> $GITHUB_OUTPUT
          echo "=================================================="

      - name: 📡 CSV Availability Check
        if: steps.check-data.outputs.skip_analysis != 'true'
        run: |
          echo "=================================================="
          echo "📡 STEP 1: CSV AVAILABILITY CHECK"
          echo "=================================================="
          echo ""
          echo "⚠️  CSV FILE IS THE CORE PREREQUISITE"
          echo "   → Without CSV, analysis cannot proceed"
          echo "   → Workflow runs hourly (4-9 PM Beijing) until CSV is available"
          echo ""
          echo "Current time: $(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M:%S %Z')"
          echo "Next retry: In 1 hour (if CSV not available)"
          echo ""
          echo "Checking Polygon Flat Files for today's CSV..."
          echo ""

      - name: 📜 Generate historical data (if requested)
        if: ${{ github.event.inputs.force_generate == 'true' && steps.check-data.outputs.skip_analysis != 'true' }}
        env:
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          POLYGON_S3_ACCESS_KEY: ${{ secrets.POLYGON_S3_ACCESS_KEY }}
          POLYGON_S3_SECRET_KEY: ${{ secrets.POLYGON_S3_SECRET_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          echo "=================================================="
          echo "📜 GENERATING HISTORICAL DATA"
          echo "=================================================="
          echo ""

          # 判断使用哪种模式
          if [ "${{ github.event.inputs.days_back }}" != "0" ] && [ "${{ github.event.inputs.days_back }}" != "" ]; then
            echo "模式: 生成过去 ${{ github.event.inputs.days_back }} 个交易日"
            python generate_historical_data.py --days ${{ github.event.inputs.days_back }}
          elif [ -n "${{ github.event.inputs.start_date }}" ]; then
            if [ -n "${{ github.event.inputs.end_date }}" ]; then
              echo "模式: 生成日期区间 ${{ github.event.inputs.start_date }} 至 ${{ github.event.inputs.end_date }}"
              python generate_historical_data.py --start "${{ github.event.inputs.start_date }}" --end "${{ github.event.inputs.end_date }}"
            else
              echo "模式: 生成单个日期 ${{ github.event.inputs.start_date }}"
              python generate_historical_data.py --date "${{ github.event.inputs.start_date }}"
            fi
          else
            echo "❌ 错误: 必须指定 days_back 或 start_date"
            exit 1
          fi

          echo ""
          echo "✓ 历史数据生成完成"
          echo ""

      - name: 🔍 Run anomaly detection
        if: steps.check-data.outputs.skip_analysis != 'true'
        env:
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          POLYGON_S3_ACCESS_KEY: ${{ secrets.POLYGON_S3_ACCESS_KEY }}
          POLYGON_S3_SECRET_KEY: ${{ secrets.POLYGON_S3_SECRET_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          FORCE_OVERWRITE: ${{ github.event.inputs.force_overwrite }}
        run: |
          echo "=================================================="
          echo "🔍 STEP 2: ANOMALY DETECTION & ANALYSIS"
          echo "=================================================="
          python main.py

      - name: 📚 Generate archive index (always update)
        run: |
          echo "=================================================="
          echo "📚 GENERATING ARCHIVE INDEX"
          echo "=================================================="
          python3 -c "
          import sys
          sys.path.insert(0, 'src')
          from archive_index_generator import get_archived_reports, generate_archive_index
          reports = get_archived_reports('output')
          generate_archive_index(reports, 'output/archive.html')
          print(f'✓ Archive index generated: {len(reports)} reports')
          "
          echo ""

      - name: 📊 Upload analysis results
        uses: actions/upload-artifact@v4
        with:
          name: analysis-results-${{ steps.get-date.outputs.date }}
          path: |
            output/
            !output/.gitkeep
          retention-days: 90

      - name: 🤖 AI Analysis & Email Notification
        if: success() && github.event_name != 'push' && steps.check-data.outputs.skip_analysis != 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWD: ${{ secrets.GMAIL_APP_PASSWD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
        run: |
          echo "=================================================="
          echo "🤖 AI ANALYSIS & EMAIL NOTIFICATION"
          echo "=================================================="
          echo ""

          # 检查是否生成了新数据
          if [ ! -f "output/NEW_DATA_GENERATED" ]; then
            echo "⊘ No new data generated (data already existed)"
            echo "   → Skipping email notification to avoid duplicates"
            echo "=================================================="
            exit 0
          fi

          echo "✓ New data generated, proceeding with email notification"
          echo ""

          # 检查是否配置了 AI 或邮件
          HAS_AI=false
          HAS_EMAIL=false

          if [ -n "$OPENAI_API_KEY" ]; then
            echo "✓ OpenAI API Key configured"
            HAS_AI=true
          else
            echo "⊘ OpenAI API Key not configured (skipping AI analysis)"
          fi

          if [ -n "$GMAIL_USER" ] && [ -n "$GMAIL_APP_PASSWD" ]; then
            echo "✓ Gmail credentials configured"
            HAS_EMAIL=true
          else
            echo "⊘ Gmail not configured (skipping email)"
          fi

          echo ""

          # 如果两者都未配置，跳过
          if [ "$HAS_AI" = "false" ] && [ "$HAS_EMAIL" = "false" ]; then
            echo "⚠️  Neither AI nor Email configured, skipping this step"
            exit 0
          fi

          # 运行 AI 分析和邮件发送
          echo "Running AI analysis and email sender..."
          python scripts/run_ai_email.py

          echo ""
          echo "=================================================="

  # Job 2: 网页部署
  deploy:
    name: 🚀 Deploy to GitHub Pages
    needs: fetch-and-analyze
    if: success()
    runs-on: ubuntu-latest

    permissions:
      contents: write  # Needed for pushing to gh-pages

    steps:
      - name: 📥 Download analysis results
        uses: actions/download-artifact@v4
        with:
          name: analysis-results-${{ needs.fetch-and-analyze.outputs.analysis-date }}
          path: output

      - name: 🚀 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output
          publish_branch: gh-pages
          keep_files: true  # Keep historical data files
          commit_message: '📊 Update report - ${{ needs.fetch-and-analyze.outputs.analysis-date }}'
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          enable_jekyll: false

      - name: ✅ Deployment summary
        run: |
          echo "=================================================="
          echo "✅ DEPLOYMENT COMPLETE"
          echo "=================================================="
          echo "📅 Analysis Date: ${{ needs.fetch-and-analyze.outputs.analysis-date }}"
          echo "🔗 Report URL: https://onlinefchen.github.io/options-anomaly-detector/"
          echo "📦 Files deployed: $(ls output | wc -l)"
          echo "=================================================="
